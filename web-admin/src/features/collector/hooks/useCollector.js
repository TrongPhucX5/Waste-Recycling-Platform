// Hook nhận/gửi trạng thái, xác nhận, lịch sử
import { useState } from "react";
import { db } from "../../services/firebaseConfig";

export const useCollector = () => {
  // ... các hàm nhận/gửi trạng thái, xác nhận, lịch sử
  return {};
};
